# 更新日志

## 1.3.0（2026-09-12）

**时间戳规范（新增，通用化）**

- 时间戳锚点由"段落前"上移到**章／节标题末尾**；段落前不再标时间戳（避免持续打断阅读）
- 区间取该层级所辖段落范围；定位精度随层级走，需要更细就补 `###` 子标题
- 颗粒度按**内容单元**决定（教学环节／话题／一轮问答／一个论点），1–3 分钟仅作参考，非硬标准
- 关键节点（可选）用 `【MM:SS】` 标在**段内**，标在段首即与标题区间重复
- 无时间戳来源的纯文本：不标注，改用环节序号组织

**访谈／播客类（新增专属口径）**

- 段首标 `**说话人**：`，便于问答切换与按人检索
- 说话人切换／话题切换／标题层级变化为**必切点**
- 说话人交替处的极短插话不并入相邻段（会破坏主体归属）
- 章节之间**不加分隔线**，正文保持连续

**其他**

- 文末新增「`【?】` 存疑标记汇总」表体例（序号｜原文（ASR）｜整理采用｜存疑原因）
- 新增 `scripts/`：段落骨架抽取、映射重建、层级化重构、去掉章间分隔线、三项核验脚本
- 新增 `README.md` 与本文件
- 备份策略改为"用户指定目录；未指定则同目录 `<原文件名>.bak`"，不再写死路径
- 移除文档中的本机路径、环境特定坑点与个人化示例

> 更早版本的变更见仓库提交历史。
